<div class="announce">
  <span class="new">new</span>
  <span>real-time balances are now live in every plan</span>
  <span class="arrow">→</span>
</div>

<style>
  .announce {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    padding: 9px 20px;
    background: var(--dm-ink);
    color: var(--dm-dark-text);
    font-family: var(--dm-font-mono);
    font-size: 12.5px;
    letter-spacing: .01em;
  }
  .new { color: var(--dm-accent-soft); font-weight: 600; }
  .arrow { color: var(--dm-faint); }
</style>
